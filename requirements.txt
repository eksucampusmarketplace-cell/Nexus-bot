python-telegram-bot>=20.7
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
asyncpg>=0.29.0
supabase>=2.3.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
httpx>=0.25.2
aiofiles>=23.2.1
cryptography>=42.0.0
yt-dlp>=2024.12.6
ffmpeg-python>=0.2.0
qrcode>=7.4.2
pillow>=11.0.0
pyrogram>=2.0.106
tgcrypto>=1.2.5
py-tgcalls>=2.1.0
# Note: py-tgcalls requires the ntgcalls native binary which may not be available
# on all platforms (e.g., Render free/hobby plans). If ntgcalls is missing,
# music streaming features will be gracefully disabled. To enable:
# 1. Ensure ffmpeg is installed
# 2. Install py-tgcalls on a system that supports native binaries (WSL, local PC)
# 3. Run the music worker on your local PC with the standalone music_worker_local.py
redis>=5.0.4
aioredis>=2.0.1
psutil>=5.9.8
pytz>=2023.3
emoji>=2.10.0
croniter>=2.0.0
# Note: py-tgcalls (formerly pytgcalls) requires ntgcalls binary dependency
# On Render, Python 3.11+ is supported with py-tgcalls
